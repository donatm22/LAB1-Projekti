#!/usr/bin/env node

/**
 * Email setup helper.
 * Prisma migrations now own the schema, so this script only points
 * developers to the standard Prisma workflow.
 */

console.log("Email setup is handled by Prisma migrations.");
console.log("");
console.log("Run the backend Prisma workflow instead:");
console.log("1. npm run generate");
console.log("2. npm run migrate");
console.log("3. npm start");
